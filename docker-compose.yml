services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: hopik4kids
      POSTGRES_USER: hopik
      POSTGRES_PASSWORD: hopik
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
