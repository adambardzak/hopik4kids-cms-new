# nginx site for the Hopík4Kids CMS backend API (VPS). TLS added by certbot.
# Copy to /etc/nginx/sites-available/api.hopik4kids.cz and symlink into sites-enabled.
server {
    server_name api.hopik4kids.cz;

    # Media uploads (article covers).
    client_max_body_size 20M;

    location / {
        proxy_pass http://127.0.0.1:18095;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port $server_port;

        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    listen 80;
}
